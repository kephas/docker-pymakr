# Run Pycom's pymakr despite its dependency issues

To run pymakr with this setup, once cloned and in the repo, you have to setup the Docker image:

    $ sudo ./build-docker.sh

Then, to run pymakr, you need to do:

	$ sudo ./run-docker.sh
